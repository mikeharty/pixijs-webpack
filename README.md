# PixiJS + Webpack Starterpack
This will set up a basic [PixiJS](https://pixijs.com/) application built and served by [Webpack](https://webpack.js.org/).


For development at `http://localhost:8080`:

```npm run start```

To build:

```npm run build```